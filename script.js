let imageElement = document.getElementById('selectedImage');
let selectedAreaElement = document.getElementById('selectedArea');
let dimensionsElement = document.getElementById('dimensions');
let textOverlay = document.getElementById('textOverlay');
let textArea = document.getElementById('textArea');

let selectedRectangle = {
    startX: 0,
    startY: 0,
    width: 0,
    height: 0
};

function handleImageUpload(event) {
    // Handle image upload logic here
}

function handleCSVUpload(event) {
    // Handle CSV upload logic here
}

function updateSelectedArea() {
    // Update the selected rectangle dimensions and display them
}

function downloadCSV() {
    // Download CSV file logic here
}

function downloadImageWithText() {
    // Download image with text logic here
}
